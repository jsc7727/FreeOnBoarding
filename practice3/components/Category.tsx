import { AttributesType } from 'common/frontMatter';
import Link from 'next/link';

type CategoryProps = {
  Category: string;
};

const Category = () => {
  return <div></div>;
};
export default Category;
